# Career / LinkedIn

Not installed on this machine. Upstream documents a LinkedIn channel
(linkedin-scraper-mcp) — deliberately not set up. This file is a stub:
`agent-reach doctor` recreates it if deleted, but preserves existing files.

For basic public LinkedIn content, Jina Reader sometimes works:

```bash
curl -s "https://r.jina.ai/https://linkedin.com/in/username"
```
