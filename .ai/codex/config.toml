# Codex CLI configuration
# Symlinked to ~/.codex/config.toml

model = "o4-mini"

# "suggest" = ask before running commands
# "auto-edit" = auto-approve file edits, ask for commands
# "full-auto" = auto-approve everything
approval_policy = "suggest"
