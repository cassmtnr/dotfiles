# Ghostty configuration
# Docs: https://ghostty.org/docs/config/reference
# List themes: ghostty +list-themes | List fonts: ghostty +list-fonts
# Reload: Cmd+Shift+,

# --- Font ---
font-family = "Fira Code"
font-size = 14
font-thicken = true  # macOS font rendering enhancement

# --- Theme ---
# For light/dark auto-switching: theme = light:Nord Light,dark:Nord
theme = Nord

# --- Window ---
window-padding-x = 4  # in points
window-padding-y = 2
window-padding-balance = true  # rebalance padding on resize
window-save-state = always
window-inherit-working-directory = true
window-new-tab-position = end

# --- Cursor ---
cursor-style = block
cursor-style-blink = true

# --- Shell Integration ---
shell-integration = detect
shell-integration-features = cursor,sudo,title

# --- Clipboard ---
clipboard-read = allow
clipboard-write = allow
clipboard-trim-trailing-spaces = true
clipboard-paste-protection = false

# --- Scrollback ---
scrollback-limit = 10000000  # ~10MB, allocated lazily

# --- Key Bindings: Tab Navigation ---
keybind = cmd+one=goto_tab:1
keybind = cmd+two=goto_tab:2
keybind = cmd+three=goto_tab:3
keybind = cmd+four=goto_tab:4
keybind = cmd+five=goto_tab:5
keybind = cmd+six=goto_tab:6
keybind = cmd+seven=goto_tab:7
keybind = cmd+eight=goto_tab:8
keybind = cmd+nine=goto_tab:9

# --- Key Bindings: Text Navigation ---
keybind = alt+left=text:\x1b\x62    # word backward
keybind = alt+right=text:\x1b\x66   # word forward
keybind = cmd+left=text:\x01         # line start
keybind = cmd+right=text:\x05        # line end
keybind = cmd+k=clear_screen

# --- macOS ---
macos-icon = custom
macos-custom-icon = ~/dotfiles/.ghostty/icon.icns
macos-titlebar-style = native
macos-option-as-alt = true  # send escape sequences instead of special characters

# xterm-256color for SSH compatibility (xterm-ghostty terminfo often missing on remote hosts)
term = xterm-256color

# --- Misc ---
confirm-close-surface = true  # prompt before closing with running processes
mouse-hide-while-typing = true
link-url = true  # clickable URLs
keybind = shift+enter=text:\x1b\r
